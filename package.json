{
  "name": "randochat-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env index.js",
    "start": "node --env-file-if-exists=.env index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.0",
    "socket.io": "^4.7.5"
  }
}
