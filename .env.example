PORT=4000

# Optional: persist /report events to Supabase.
# Find these in Supabase Dashboard → Project Settings → API.
# Use the service_role key (server only, never commit).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
